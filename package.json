{
  "name": "fde-context-platform",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.0.8",
  "scripts": {
    "prepare": "git config core.hooksPath .githooks || true",
    "dev": "tilt up",
    "dev:down": "tilt down",
    "build": "turbo run build",
    "clean": "turbo run clean && rm -rf .turbo node_modules/.cache",
    "typecheck": "tsc -b && pnpm --filter @fde/web run typecheck && pnpm --filter @fde/e2e run typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "pnpm --filter @fde/e2e run e2e",
    "e2e:stack:up": "docker compose -f docker-compose.e2e.yml up -d --wait",
    "e2e:stack:down": "docker compose -f docker-compose.e2e.yml down -v",
    "e2e:db": "e2e/scripts/db-setup.sh",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "review": "pnpm --filter @fde/review start",
    "db:bootstrap": "sh -c 'set -a; [ -f .env ] && . ./.env; set +a; pnpm --filter @fde/db run db:bootstrap'",
    "db:harden": "sh -c 'set -a; [ -f .env ] && . ./.env; set +a; pnpm --filter @fde/db run db:harden'",
    "db:generate": "pnpm --filter @fde/db run db:generate",
    "db:migrate": "tsx --env-file-if-exists=.env packages/db/src/migrate.ts"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "catalog:",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "catalog:",
    "typescript-eslint": "^8.19.1",
    "vitest": "catalog:"
  }
}
