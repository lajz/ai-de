/** @type {import('next').NextConfig} */
const nextConfig = {
  // The web app is a thin read-only view over `@fde/api`; nothing to configure yet.
  reactStrictMode: true,
};

export default nextConfig;
